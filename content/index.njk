---js
const eleventyNavigation = {
	key: "Home",
	order: 1
};
---
{% for tag in collections | getKeys | filterTagList | sortAlphabetically %}
    {% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
    <a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>
{% endfor %}

/

<a href="https://kernel.org/">Kernel Archives</a>
<a href="https://nobaraproject.org/">Nobara Linux</a>
<a href="https://www.chezmoi.io/user-guide/command-overview/">chezmoi dotfiles</a>
<a href="https://starship.rs/presets/">Starship shell prompt</a>
<a href="https://neovim.io/">Neovim</a>


{{ "" | markdown | safe }}

{% set list = [] %}

{# Build a filtered list manually — Nunjucks-compatible #}
{% for p in collections.posts | reverse %}
  {% if not p.data.draft %}
    {% set list = list.concat([p]) %}
  {% endif %}
{% endfor %}

{# Now show only first 10 #}
<ul>
{% for post in list | head(10) %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    <time datetime="{{ post.date | htmlDateString }}">
      / {{ post.date | readableDate("dd LLL yyyy") }}
    </time>
  </li>
{% endfor %}
</ul>
